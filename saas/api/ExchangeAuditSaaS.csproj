<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ExchangeAuditSaaS</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.IO.Packaging" Version="8.0.0" />
  </ItemGroup>

  <!-- Shared audit DSL (AuditSection / AuditOptionGroup / AuditSelection /
       ScriptContext plus all Exchange Online section builders).
       Default glob compiles Shared/*.cs. -->

</Project>
