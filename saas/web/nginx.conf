server {
  listen 80;
  root /usr/share/nginx/html;
  location /api/ { proxy_pass http://api:8080; proxy_set_header Authorization $http_authorization; proxy_set_header X-Tenant-Id $http_x_tenant_id; }
  location / { try_files $uri $uri/ /index.html; }
}
